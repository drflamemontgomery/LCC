(defun main: int
  ((argc :int) (argv :**const char))
  nil
  t
  (+ 1 2)
  (cond 
    ((eql 1 2)
     (t (return 1))))
  (case '3'
    (('\050' (return '\x4a'))))
  (for :i64 ((a 0)) (lt a 5) (inc a)))

(defun let-test: *const i32 ((a :i32)) (let ((a :i32 5) (b :f32 4) (c:*const u8 2)) t))
(defun true :bool () t)
(defun false :bool () nil 2)
(defun while-test :void ()
  (do-while nil (printf "do-while\n"))
  (while nil (printf "while\n")))
